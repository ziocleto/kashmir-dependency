CMAKE_MINIMUM_REQUIRED(VERSION 2.6.0)
PROJECT(kashmir)

ADD_LIBRARY(kashmir cli.cpp)

INSTALL (FILES kashmir/* DESTINATION "${CMAKE_INSTALL_INCDIR}/kashmir/")
INSTALL (TARGETS kashmir LIBRARY DESTINATION "${CMAKE_INSTALL_LIBDIR}")
